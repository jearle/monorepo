export const UNIT_YEARS = `y`;
export const UNIT_QUARTERS = `q`;
export const UNIT_MONTHS = `M`;
export const UNIT_WEEKS = `w`;
export const UNIT_DAYS = `d`;
export const UNIT_BUSINESS_DAYS = `bd`;
export const UNIT_HOURS = `h`;
export const UNIT_MINUTES = `m`;
export const UNIT_SECONDS = `s`;
export const UNIT_MILLISECONDS = `ms`;

export const UNITS = [
  UNIT_YEARS,
  UNIT_QUARTERS,
  UNIT_MONTHS,
  UNIT_WEEKS,
  UNIT_DAYS,
  UNIT_BUSINESS_DAYS,
  UNIT_HOURS,
  UNIT_MINUTES,
  UNIT_SECONDS,
  UNIT_MILLISECONDS,
] as const;
