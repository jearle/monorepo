{
  "name": "@jearle/util-api",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    },
    "./*": {
      "import": "./src/*/index.ts",
      "types": "./src/*/index.ts"
    }
  },
  "scripts": {
    "format": "bun run repo package format",
    "compile": "bun run repo package compile",
    "lint": "bun run repo package lint",
    "verify": "bun run repo package verify"
  },
  "dependencies": {
    "@jearle/schema-api": "workspace:*",
    "@jearle/schema-error": "workspace:*"
  },
  "peerDependencies": {
    "lodash.round": "^4"
  },
  "devDependencies": {
    "@jearle/config-oxlint": "workspace:*",
    "@jearle/config-tsconfig": "workspace:*"
  }
}
