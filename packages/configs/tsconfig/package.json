{
  "name": "@jearle/config-tsconfig",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "files": [
    "tsconfig.json"
  ],
  "exports": "./tsconfig.json",
  "scripts": {
    "format": "bun run repo package format",
    "compile": "bun run repo package compile",
    "lint": "bun run repo package lint"
  },
  "dependencies": {},
  "devDependencies": {
    "@jearle/config-oxlint": "workspace:*"
  }
}
