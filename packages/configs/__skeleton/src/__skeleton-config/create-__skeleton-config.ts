import { __SKELETON_CONFIG_NAME } from './constants';
import { type Create__SkeletonConfigResult } from './types';

export const create__skeletonConfig = () => {
  const __skeletonResult: Create__SkeletonConfigResult = {
    name: __SKELETON_CONFIG_NAME,
  };
  const result = __skeletonResult;

  return result;
};
