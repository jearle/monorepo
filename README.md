# Odva Monorepo

## Quick Start

**Linux/Macos**

```bash
git clone git@github.com:odvaai/odva.git
curl -fsSL https://bun.sh/install | bash
source ~/.zshrc
packages/apps/odva-home-web
bun -v # verify version installed correctly
bun install
cd packages/apps/odva-home-web
bun start
```
